import { createSlice } from '@reduxjs/toolkit'

const toolboxSlice = createSlice({
	name: 'toolbox',
	initialState: {},
	reducers: {},
})

export const {} = toolboxSlice.actions
export default toolboxSlice.reducer
